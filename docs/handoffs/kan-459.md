# KAN-459 — H1 membership billing foundation handoff

## Status

KAN-459 implementation is complete and is in story-closure verification on branch `feat/KAN-459-membership-billing-foundation`. KAN-465 through KAN-470 are complete; KAN-471 owns the final story gate, durable documentation, Jira closure and integration back to `dev`.

Do not treat this handoff as evidence that the final KAN-471 gate or merge has already passed. Record those results only after they actually run.

## Delivered behavior

H1 introduces temporal `TeamEconomicPolicy`, temporal `AthleteBillingTerms` and immutable `MonthlyCharge` snapshots with equivalent SQLite/Supabase persistence. Team defaults are prospective configuration; athlete terms preserve the condition actually applied; monthly materialization is idempotent and explicit.

The Coach UI adds localized ES/EN membership management. Team policy and athlete terms are shown as current/scheduled/history timelines. Existing materialized charges are read-only on the athlete surface; page navigation does not materialize charges.

## Durable invariants

- Money uses integer minor units. The initial ARS 25,000 / day-5 values are configuration, not universal constants.
- Team policies are temporal and month-boundary based.
- Initial athlete terms may start any day; ordinary replacements are prospective month-boundary changes.
- Any reached portion of a month produces the full monthly fee; no H1 proration.
- Only one billing-terms interval may claim an athlete/month; persistence uniquely protects `(athleteId, year, month)`.
- Materialized charges are historical snapshots and are not rewritten by later prospective changes.
- Materialization is an application operation independent of UI navigation.
- Legacy `memberships` is preserved but is not economic authority; ambiguous legacy rows are not automatically converted into economic history.
- Team/athlete isolation is checked at application/persistence boundaries.
- H1 does not implement H2+ reductions, due-date exceptions/extensions, payments, derived account status/balance, notices or Athlete blocking.

The durable domain contract is `docs/architecture/memberships.md`.

## Persistence baseline

SQLite uses migration `drizzle/sqlite/0008_membership_billing_foundation.sql`. Supabase uses `drizzle/supabase/0023_membership_billing_foundation.sql`. Both define the three H1 tables and athlete/month uniqueness; Supabase additionally participates in the existing RLS verification lifecycle.

Production Supabase migration remains a controlled deployment concern: a versioned migration or local check is not evidence that production was migrated.

## T6 evidence already established

Before KAN-471 documentation reconciliation:
- `pn test`: 1386 tests, 216 suites, 1386 pass, 0 fail.
- `pn lint`: 0 errors / 0 warnings after fixture cleanup.
- `pn tsc`: GREEN during the T6 gate.
- `pn i18n:check`: 721 aligned message leaves.
- Coach ES/EN manual walkthrough: GREEN on membership and athlete membership surfaces, with no final console warnings.

The Windows portable test runner was hardened during T6 to batch large inventories and emit one aggregate summary, eliminating the observed `ENAMETOOLONG` failure.

These are T6 evidence, not a substitute for the final KAN-471 full gate after documentation reaches its final HEAD.

## KAN-471 remaining closure work

Run the final story gate from the definitive HEAD: `pn test`, `pn lint`, `pn tsc`, `pn build`, `pn i18n:check`, and the applicable SQLite/Supabase checks/verifiers required by the H1 schema changes. Reconcile the results against KAN-459 acceptance criteria and record only commands that actually ran.

After all gates are GREEN, record KAN-471 and story-level Jira evidence, transition KAN-471 then KAN-459 to Finalizada, and merge the story branch back to current `dev` according to `AGENTS.md`.

## KAN-460 entry contract

KAN-460 may assume H1's policy → athlete terms → monthly snapshot chain and must extend it without rewriting H1 history. H2 owns reductions/scholarships, global monthly due-date exceptions and individual extensions. Its due-date precedence is team policy → global monthly exception → `baseDueDate` → individual extension → `effectiveDueDate`. Reductions must remain traceable charge adjustments rather than fake payments.

Bootstrap KAN-460 from the merged current `dev`, this handoff as navigation, `docs/architecture/memberships.md`, focused current code/tests and the complete Jira issue/dependencies.
