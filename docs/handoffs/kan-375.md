# KAN-375 handoff — Athlete legacy ES/EN localization

Status: closure verification in progress on branch `kan-375-athlete-legacy-i18n`.

## Delivered

- Athlete list, empty state and table visible copy use canonical `next-intl` messages.
- Athlete detail visible copy uses the `AthleteDetail` namespace, including personal/contact data, goals, competitions, planning resolution, emergency contact and the 1000 m panel states covered by this story.
- Athlete group assignment/change page and form use the `AthleteGroup` namespace.
- New Athlete message fragments are explicitly registered and statically loadable for both `en` and `es`.
- Persisted user/domain text remains data; KAN-375 does not translate stored content.

## Focused evidence

Human local RED/GREEN evidence was recorded in Jira for KAN-420, KAN-421, KAN-422 and KAN-423. The strengthened KAN-423 Athlete-detail contract is GREEN at implementation HEAD `ecb3908b9e8544bc5f4930640cc68ae742bddfd8`.

The manual re-walkthrough of the previously failing English Athlete-detail surface is GREEN.

## Closure gate

A full gate was GREEN earlier in KAN-423 at 1113/1113 tests across 205 suites, lint/typecheck/build GREEN and 590 aligned i18n message leaves. Because Athlete-detail implementation changed after that gate, it must be rerun at the final documented revision before integration.

No persistence or schema changes belong to KAN-375, so DB/Supabase verification is not applicable.

## Deferred / separate scope

The closure walkthrough exposed broader legacy localization debt in Coach Groups, Cohorts, Planning and Sessions. Those findings are recorded under KAN-408 and are outside KAN-375.

The walkthrough also exposed unexpected Product-maintained competition catalog entries. Their provenance and intended visibility must be investigated under KAN-408 before any data mutation or deletion.

Athlete Profile remains a mockup and was explicitly excluded from this walkthrough.

## Integration

Do not treat this handoff as proof of merge. KAN-423 still requires the final post-change full gate, final Jira evidence, integration to `dev`, remote verification and final Jira transitions.
