# KAN-414 handoff

Status: **closure candidate in progress on story branch**.

Story branch: `kan-414-close-stabilized-baseline`.

## Purpose

KAN-414 closes the stabilized KAN-408 baseline with reproducible technical, functional, documentation and administrative evidence. This handoff records only evidence established during the closure story; historical story gates remain historical.

## Reconciled baseline

- KAN-455 audited the KAN-408 work-item history and baseline evolution.
- KAN-456: human-reported `pn db:sqlite:check` and `pn db:supabase:check` GREEN. This does not claim a Supabase production migration or remote deployment verification.
- The canonical SQLite aggregate gate has nine scenarios, including the real base-seed regression introduced by KAN-454.
- `GroupSessionPrescription.durationMin` is explicit positive-or-null coach-planned group duration. Trail/Hills may display it but do not derive athlete-facing pace/speed or individual predicted duration from it.
- The canonical intensity contract is `reference_percentage` / `referencePercentage`; Z1-Z5 remains independent.

## KAN-457 walkthrough evidence

Human-confirmed ES/EN walkthrough:

- Sessions: Workout Type localization, date-driven microcycle assignment, reference-percentage selector, and Trail/Hills group-duration presentation behaved as intended.
- Athlete 1000 m: official registration, date eligibility, self-directed submission, duplicate-official protection, localized errors and post-submit refresh were exercised.
- Coach 1000 m: Accept/Reject/Correct lifecycle was exercised, including automatic UI refresh.
- Rejected evidence remains durable but is excluded from the Coach operational history and from analytical eligibility.
- Coach correction is append-only. A correction made through the Coach boundary is authoritative and the replacement is accepted even when the original evidence was self-directed.
- Observed 1000 m duration is displayed explicitly as `N min SS s`; derived RunningReference may use pace units such as `min/km`.
- Athlete Stats ES/EN was checked for evidence/reference/evolution separation, lifecycle presentation, responsive behavior and localization.

## Focused regressions added during closure

The closure walkthrough added focused coverage for:

- Coach and Athlete post-registration refresh.
- Sequential official-TestEvent idempotency in application/repository boundaries.
- Athlete duplicate-official localization.
- Rejected evidence exclusion from Coach operational history.
- Coach correction dialog/refresh.
- Coach correction lifecycle and observed-duration presentation.

The official-TestEvent duplicate guard is an application/repository sequential guarantee, not a database concurrency uniqueness guarantee.

## Documentation reconciliation

KAN-457 reconciled:

- `architecture/platform/sqlite-local-operations.md` to the nine-scenario SQLite gate.
- `architecture/monitoring/field-performance-and-execution-guidance.md` to the delivered `durationMin` semantics.
- `handoffs/kan-413.md` from pending integration to historical integrated state.
- this handoff and the documentation index to KAN-414 as the active closure baseline.

## Final closure-candidate gate

Human-reported current-branch evidence:

- `pn test`: **1245/1245 pass**, **216 suites**, 0 fail/cancelled/skipped/todo.
- `pn lint`: GREEN.
- `pn tsc`: GREEN.
- `pn build`: GREEN.
- `pn i18n:check`: GREEN, **721** aligned message leaves.
- `pn db:sqlite:check`: GREEN.
- `pn db:supabase:check`: GREEN.

This is current KAN-414 closure-candidate evidence rather than inherited historical evidence. Supabase production migration 0022 remains a controlled-deployment item unless explicit remote evidence is produced.

## Closure remaining

Audit the KAN-414 acceptance criteria, reconcile any remaining debt, integrate the story branch into current `dev`, and verify remote branch identity before closing KAN-414 and KAN-408.
