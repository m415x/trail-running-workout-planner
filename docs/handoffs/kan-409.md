# KAN-409 handoff — baseline, gates and static analysis

Status: closure verification in progress on branch `kan-409-reconcile-baseline-gates-static-analysis`.

## Delivered

- Portable test discovery no longer depends on shell `**` glob expansion.
- Canonical typecheck prepares Next route types before `tsc --noEmit`.
- The focused GREEN TDD path reuses that canonical typecheck runner through the project-local `tsx` CLI.
- The audited ESLint baseline is clean: 0 errors / 0 warnings.
- KAN-408 baseline evidence distinguishes historical results, local migration-journal consistency and current remote Supabase verification.

## Verified closure evidence

Human local evidence on the closure branch:

- `pn test`: 1107 tests, 202 suites, 1107 pass, 0 fail.
- `pn lint`: GREEN.
- `pn tsc`: GREEN; Next route types generated successfully.
- `pn build`: GREEN.
- `pn i18n:check`: 532 message leaves aligned.
- `pn db:check:supabase`: migration journal consistent.
- `pn db:verify:supabase`: 38/38 application tables and 38/38 tables with RLS; field-performance lifecycle and workout-log timing columns OK.
- Focused KAN-419 TDD runner verification: GREEN after routing canonical typecheck through `tsx`.

Counts are revision-specific evidence, not permanent contracts.

## Historical reconciliation

Epic 4 closure remains valid historical evidence for its revision: 1098 tests / 202 suites, lint 0 errors / 9 warnings, typecheck/build GREEN, i18n 532, Supabase 38/38 with RLS. The initial KAN-408 result of 936 tests / 195 suites was traced to shell-dependent test discovery rather than deleted tests.

## Deferred / separate scope

- KAN-407 owns SQLite local upgrade-path reliability; KAN-409 does not absorb it.
- Other deferred Epic 4 work remains governed by current Jira and `docs/history/epic-4.md`.

## Integration

Do not treat this handoff as proof of merge. KAN-419 still requires final acceptance/documentation review, merge to `dev`, remote `dev` verification and Jira closure transitions.
