# KAN-410 handoff

Status: **completed and integrated into `dev`**.

Final integration baseline: `5dfb36d035ce38e724a18b78c7b77371f1cc0bd8`.

## Delivered behavior

- `WORKOUT_TYPES` is the canonical persisted/domain workout-type catalog; consumers do not maintain divergent full catalogs.
- Structural workout-type metadata is separate from localized ES/EN labels.
- Session create, list, calendar, detail and edit surfaces use the modular `next-intl` catalogs.
- Session server actions and prescription parsing return stable error codes; user-facing messages are translated at the SessionForm boundary.
- Session form options return structural microcycle data and the UI composes the localized option label.
- Existing persisted workout-type values and active/archived workout-template behavior remain compatible.

## Current invariants

- Persisted workout-type identifiers are locale-independent.
- Domain/action validation does not own localized product copy.
- Workout-type labels come from `Workouts.types.*`.
- Session product copy belongs to the modular `Sessions` catalog.
- Z1-Z5 and PAM-related legacy semantics were not redefined by KAN-410.
- Microcycle auto-resolution and `durationMin` semantics remain outside KAN-410.

## Verified closure evidence

Human local execution of the complete story gate was GREEN:

- `pn test`: 1183/1183 passed across 205 suites, 0 failed.
- `pn lint`: GREEN.
- `pn tsc`: GREEN.
- `pn i18n:check`: 713 message leaves aligned.
- `pn build`: GREEN on Next.js 16.3.2.
- ES/EN functional walkthrough: GREEN for list/calendar, create, detail and edit, including the required localization/template checks.

The persistent KAN-410 story branch was fast-forward integrated into `dev`; post-integration comparison was identical (ahead 0 / behind 0) at the baseline SHA above.

## Next-story navigation

KAN-410 deliberately left these Jira-owned follow-ups outside its scope:

- KAN-411: resolve microcycle from session date.
- KAN-412: reconcile the legacy intensity percentage contract.
- KAN-413: clarify `durationMin` semantics.

These are navigation pointers, not an instruction to start any issue without reading current Jira state. A fresh chat must bootstrap from current `dev`, `AGENTS.md`, `docs/README.md`, this handoff where relevant, focused domain docs/code/tests, and then the complete candidate Jira story before proposing work.
